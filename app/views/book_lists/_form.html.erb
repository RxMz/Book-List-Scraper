<%= form_with(model: book_list, local: true) do |form| %>
  <% if book_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_list.errors.count, "error") %> prohibited this book_list from being saved:</h2>

      <ul>
      <% book_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term_id %>
    <%= form.text_field :term_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
