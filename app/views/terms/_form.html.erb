<%= form_with(model: term, local: true) do |form| %>
  <% if term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(term.errors.count, "error") %> prohibited this term from being saved:</h2>

      <ul>
      <% term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term_code %>
    <%= form.number_field :term_code %>
  </div>

  <div class="field">
    <%= form.label :term_description %>
    <%= form.text_field :term_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
